# Local Development Notes

## Server Management

The dev server is user-controlled. **Do not start, stop, or restart the server automatically.**

When verifying UI changes, describe what to check and let the user refresh/test in their browser. If a restart is genuinely required (e.g. backend code changed), say so explicitly and let the user run it:

```bash
./run.sh
```
